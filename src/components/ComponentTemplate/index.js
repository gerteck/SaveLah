import React from "react";
import { Text, View } from "react-native";
import { styles } from './styles';

const ComponentTemplate = ({title, onPress}) => {
    return (
        <View>ComponentTemplate</View>
    )
}

export default React.memo(ComponentTemplate);